const initialState = {
  friends: [],
  update: false,
  id: null
};

export const friendsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
